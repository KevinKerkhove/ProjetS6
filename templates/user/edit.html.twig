{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>
    <div class=" row col-8">
        <a type="button" class="btn btn-outline-success col-2" href="{{ path('user_index') }}">Revenir à la liste</a>
        {{ include('user/_delete_form.html.twig') }}
    </div>
   
    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

{% endblock %}
