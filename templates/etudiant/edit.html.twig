{% extends 'base.html.twig' %}

{% block title %}Edit Etudiant{% endblock %}

{% block body %}
    <h1>Edit Etudiant</h1>
    <div class=" row col-8">
        <a type="button" class="btn btn-outline-success col-2" href="{{ path('etudiant_index') }}">Revenir à la liste</a>
        {{ include('etudiant/_delete_form.html.twig') }}
    </div>
    {{ include('etudiant/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
