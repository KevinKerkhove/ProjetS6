{% extends 'base.html.twig' %}

{% block title %}SousGroupeEtudiant{% endblock %}

{% block body %}
    <h1>SousGroupeEtudiant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sous_groupe_etudiant.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sous_groupe_etudiant_index') }}">back to list</a>

    <a href="{{ path('sous_groupe_etudiant_edit', {'id': sous_groupe_etudiant.id}) }}">edit</a>

    {{ include('sous_groupe_etudiant/_delete_form.html.twig') }}
{% endblock %}
