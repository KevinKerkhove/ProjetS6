{% extends 'base.html.twig' %}

{% block title %}SousGroupeEtudiant index{% endblock %}

{% block body %}
    <h1>SousGroupeEtudiant index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sous_groupe_etudiant in sous_groupe_etudiants %}
            <tr>
                <td>{{ sous_groupe_etudiant.id }}</td>
                <td>
                    <a href="{{ path('sous_groupe_etudiant_show', {'id': sous_groupe_etudiant.id}) }}">show</a>
                    <a href="{{ path('sous_groupe_etudiant_edit', {'id': sous_groupe_etudiant.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('sous_groupe_etudiant_new') }}">Create new</a>
{% endblock %}
