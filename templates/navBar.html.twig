<nav class="navbar navbar-expand-sm navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('home') }}">Home</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar-collapse collapse w-100 order-1 order-md-0 dual-collapse2" id="navbarNav">
        <ul class="navbar-nav mr-auto">
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('etudiant_new') }}">Créer etudiant</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('xls_import') }}">Upload Fichier</a>
                </li>


                <li class="nav-item">
                    <a class="nav-link" href="{{ path('etudiant_index') }}">Étudiants</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('etudiant_index', {'inscrit': "inscrit"}) }}">Inscrits</a>
                </li>
            {% endif %}
            <li class="nav-item">
                    <a class="nav-link" href="{{ path('promotion_index') }}">Promotions</a>
            </li>
        </ul>
    </div>

    <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
        <ul class="navbar-nav ml-auto">
            {% if is_granted('ROLE_USER') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('user_profile', {'user': app.user.id}) }}">Profil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">logout</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">login</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>