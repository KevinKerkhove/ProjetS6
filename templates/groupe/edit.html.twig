{% extends 'base.html.twig' %}

{% block title %}Edit Groupe{% endblock %}

{% block body %}
    <h1>Edit Groupe</h1>
    <div class=" row col-8">
        <a type="button" class="btn btn-outline-success col-2" href="{{ path('groupe_index') }}">Revenir à la liste</a>
        {{ include('groupe/_delete_form.html.twig') }}
    </div>
    {{ include('groupe/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
